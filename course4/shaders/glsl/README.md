GLSL sources (`*.vert`, `*.frag`, `*.comp`) — compiled to `build/<preset>/shaders/*.spv`
by the `compile_shaders` target whenever `glslc` (Vulkan SDK) is on the PATH.
Shader code is written per-lab, starting with Lab 2.1.
