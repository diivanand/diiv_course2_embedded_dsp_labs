#include <benchmark/benchmark.h>

// Lab 0.1/0.2 — register your benchmarks here; benchmark_main provides main().
// Example registration shape (the measured code is yours to write):
//   static void BM_something(benchmark::State& state) { for (auto _ : state) { ... } }
//   BENCHMARK(BM_something);
