// Placeholder translation unit so engine_vulkan builds before Lab 0.3.
// Your Vulkan backend (instance, device, swapchain, frame loop) replaces this
// file as Labs 0.3 → 4.2 build it out.
#include <volk.h>

namespace c4::vk {
// TODO(Lab 0.3): context bring-up starts here.
}
